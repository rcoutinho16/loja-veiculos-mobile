import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'loja-veiculos-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
